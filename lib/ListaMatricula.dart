import 'package:flutter/material.dart';

class ListaMatricula extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("funciona"),),
    );
  }

}